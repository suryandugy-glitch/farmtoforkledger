const express=require('express');const r=express.Router();const c=require('../controllers/productController');r.get('/',c.getAllProducts);r.get('/:qrCode',c.getProductByQRCode);r.post('/',c.addProduct);r.delete('/:qrCode',c.deleteProduct);r.post('/:qrCode/submit-usda',c.submitToUsda);r.post('/:qrCode/usda-review',c.usdaReview);r.post('/:qrCode/verify',c.verifyProduct);module.exports=r;
